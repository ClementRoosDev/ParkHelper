<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParkHelper.Views.ItinerairePage"
             Title ="Itineraire" 
             Disappearing="ItinerairePage_OnDisappearing"
             Appearing="ItinerairePage_OnAppearing">
    <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <ListView Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center"
                x:Name="ListView" IsVisible="True"
                IsGroupingEnabled="True"
                GroupDisplayBinding="{Binding Heure}"
                GroupShortNameBinding="{Binding}">
      </ListView>
    </Grid>
</ContentPage>