<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParkHelper.Views.ItinerairePage"
             Title ="Itineraire">
  <ActivityIndicator
                        IsVisible="{Binding IsBusy}"
                        IsRunning="{Binding IsBusy}"
                        Color="Black" />
  <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="50"/>
    </Grid.RowDefinitions>
    <ListView Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center"
              x:Name="ListView" ItemsSource="{Binding Listes}"
              IsGroupingEnabled="True"
              GroupDisplayBinding="{Binding ObjectifAttraction}"
              GroupShortNameBinding="{Binding ObjectifAttraction}">
    </ListView>
    <!--
    <Button Grid.Row="1" Text="Créer itinéraire" Command="{Binding ItineraireCommand}" />-->
  </Grid>
</ContentPage>