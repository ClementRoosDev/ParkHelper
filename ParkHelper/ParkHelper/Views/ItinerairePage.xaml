<?xml version="1.0" encoding="utf-8" ?>
<NavigationPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ParkHelper.Views.ItinerairePage"
             Title ="Itineraire" Disappearing="ItinerairePage_OnDisappearing" Appearing="ItinerairePage_OnAppearing">
  <ActivityIndicator
                        IsVisible="{Binding IsBusy}"
                        IsRunning="{Binding IsBusy}"
                        Color="Black" />
  <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="50"/>
    </Grid.RowDefinitions>
    <ListView Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center"
              x:Name="ListView">
      
    </ListView>
  </Grid>
</NavigationPage>